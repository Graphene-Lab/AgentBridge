# TODO — next steps

## 1. Port the examples into the book (the main next step)

Right now the examples live only in this `examples/` folder. The next pass is to **insert
them into the book files** in `C:\Users\andre\OneDrive\Sorgenti\Automate-Your-Business-with-AI\books\EN`,
following the map in [`book_implementation.md`](book_implementation.md).

For each example:
1. Open the target chapter listed in `book_implementation.md`.
2. Copy the example's story (the "situation", "what you ask", "what it does") into the
   chapter as a short, plain-English box.
3. Copy the PNG into the book's image folder and reference it with a one-line caption.
4. Keep the "what you asked" line visible — it is the hook.

## 2. Add the proposed new chapters

Create the new chapters proposed in `book_implementation.md` and place them at the
suggested positions:
- New Part **"Meet AgentBridge"** (after `ch17`).
- New chapter **"Scheduled tasks"** (after `ch12`).
- New chapter **"Your assistant by voice and by phone"**.
- New chapter **"Working on the go with Telegram"**.

Renumber later chapters if the book's build requires sequential numbering.

## 3. Publish the free book

When the book is ready, make the GitHub repository public and confirm the download link in
`README.md` points to it:
`https://github.com/Graphene-Lab/Automate-Your-Business-with-AI`

## 4. Optional polish

- Re-capture any TUI screen after a product update so the pictures stay current.
- Add a couple of industry-specific examples (retail, healthcare) if the book needs them.
- Proofread the English for non-native simplicity across all examples.
